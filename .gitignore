# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
.env.local

# claude code — track commands/ (skills) and launch.json; keep local-only files out
.claude/settings.local.json
.claude/worktrees/

# agent workspace folders — internal reference docs, NOT for the public repo
/agent-workspaces/

# google service account keys — NEVER commit (real key lives in ~/.jc-secrets/)
*-service-account*.json
jc-website-*.json
*.serviceaccount.json
